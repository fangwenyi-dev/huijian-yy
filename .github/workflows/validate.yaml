name: Validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Hassfest validation
        run: |
          pip install git+https://github.com/home-assistant/core.git#script/hassfest
          hassfest .
